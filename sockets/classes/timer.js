class Timer {
    constructor(totalSeconds, currentSeconds, descending) {
        this.totalSeconds = totalSeconds;
        this.currentSeconds = currentSeconds;
        this.descending = descending;
    }
}
module.exports = Timer;